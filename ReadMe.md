# A simple python script to draw a white fade circle on a transparent background

I can't find free pictures of white fade circles on transparent backgrounds, so I made a simple python script to draw them.
